apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "task-app.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "task-app.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "task-app.name" . }}
    spec:
      containers:
        - name: app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
          envFrom:
            - secretRef:
                name: task-app-secret
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
